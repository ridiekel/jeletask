teletask:
  configFile: "classpath:/test-config.json"
  central:
    host: "localhost"
    port: 0
    id: "MAN_TEST_${teletask.central.host}"
    version: "@project.version@"
  mqtt:
#    host: "localhost"
#    port: 1883
#    username: ""
#    password: ""
    retained: "false"
    clientId: "test_client_id_teletask2mqtt"
    prefix: "test_prefix_teletask2mqtt"
  test:
    headless: true
    timeout: 50000
    gitlabci: false
    keeprunning: false
  log:
    topic-enabled: true
    haconfig-enabled: true
  db:
    dir: ./.data

spring:
  output:
    ansi:
      enabled: ALWAYS

logging:
  level:
    root: ERROR
    org.springframework: ERROR
    io.github.ridiekel.jeletask: DEBUG
#    io.github.ridiekel.jeletask.mqtt.listener.logic.motor.MotorProgressor: TRACE
#    io.github.ridiekel.jeletask: TRACE
